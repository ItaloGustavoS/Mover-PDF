;(function (window, document) {

    // Modernizr constructor
    var Modernizr = function () {};

    // Define flexbox method
    Object.defineProperty(Modernizr.prototype, 'flexbox', {
        enumerable: false,
        configurable: true,
        writable: true,
        value: function () {
            var a = document.createElement("div"),
                b = document.createElement("div");
            a.style.cssText = "display:box;";
            b.style.cssText = "box-flex:1;width:10px;";
            a.appendChild(b);
            document.documentElement.appendChild(a);
            var c = b.offsetWidth === 42;
            a.removeChild(b);
            document.documentElement.removeChild(a);
            return c;
        }
    });

    // Define canvas method
    Object.defineProperty(Modernizr.prototype, 'canvas', {
        enumerable: false,
        configurable: true,
        writable: true,
        value: function () {
            return !!document.createElement("canvas").getContext;
        }
    });

    // Create a new instance of Modernizr
    var modernizr = new Modernizr();

    // Export the Modernizr object to the global scope
    window.Modernizr = modernizr;

    // Return the modernizr object
    return modernizr;

})(window,
